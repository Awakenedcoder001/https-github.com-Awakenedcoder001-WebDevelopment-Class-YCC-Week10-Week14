class MethodOverloading:
    def add(self,a=none,b=none,c=none,d=none):


        if a!=none and b!=none, c!=none,d!=none:
            res=a+b+c+d
            return res

        if a!=none and b!=none and c!=none:
            res=a+b+c
            return res

        if a!=none and b!=none:
            ret